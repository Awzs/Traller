# 测试版Dockerfile - 最简化版本
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY backend/package*.json ./
COPY backend/pnpm-lock.yaml ./

# 安装pnpm和依赖
RUN npm install -g pnpm && \
    pnpm install --frozen-lockfile

# 复制源代码
COPY backend/ .

# 显示文件结构（调试）
RUN echo "=== Files before build ===" && ls -la

# 构建应用
RUN echo "=== Building application ===" && \
    pnpm run build && \
    echo "=== Build completed ==="

# 显示构建结果（调试）
RUN echo "=== Checking dist directory ===" && ls -la dist/
RUN echo "=== Checking dist/src directory ===" && ls -la dist/src/
RUN echo "=== Checking main.js ===" && ls -la dist/src/main.js

# 暴露端口
EXPOSE 8080

# 设置环境变量
ENV NODE_ENV=production
ENV PORT=8080

# 启动应用
CMD ["node", "dist/src/main.js"]
